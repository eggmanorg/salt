{
  "name": "@salt/cloud-functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.js --external:firebase-admin --external:firebase-admin/* --external:firebase-functions --external:firebase-functions/* --external:sharp --banner:js=\"import { createRequire } from 'module'; import { fileURLToPath as __saltFileURLToPath } from 'url'; import { dirname as __saltDirname } from 'path'; const require = createRequire(import.meta.url); const __filename = __saltFileURLToPath(import.meta.url); const __dirname = __saltDirname(__filename);\" && cp src/flows/assets/canon-icon-seed.webp dist/canon-icon-seed.webp && node scripts/write-deploy-package.mjs && npm install --prefix dist --omit=dev --no-audit --no-fund --loglevel=error --os=linux --cpu=$(node -p process.arch) --libc=glibc --include=optional && node scripts/add-native-sharp.mjs",
    "dev": "NODE_OPTIONS='--disable-warning=DEP0040' genkit start --port 4001 -- tsx --env-file=.secret.local --watch src/dev.ts",
    "probe": "tsx probes/run.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:emulator": "vitest run --config vitest.emulator.config.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@types/web-push": "^3.6.4",
    "esbuild": "^0.28.2",
    "genkit-cli": "^1.42.0",
    "tsx": "^4.23.12"
  },
  "dependencies": {
    "@genkit-ai/firebase": "^1.42.0",
    "@genkit-ai/google-genai": "^1.42.0",
    "@opentelemetry/sdk-trace-node": "^1.30.1",
    "@salt/domain": "workspace:*",
    "@salt/observability": "workspace:*",
    "@salt/shared-types": "workspace:*",
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "genkit": "^1.42.0",
    "resend": "^6.26.0",
    "sharp": "^0.35.4",
    "web-push": "^3.6.7"
  }
}
